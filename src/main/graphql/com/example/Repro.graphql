query Shelf($id: ID!) {
  viewer {
    shelf(id: $id) {
      id
      books {
        ...bookFragment
      }
    }
  }
}


query BooksByIds($ids: [ID!]!) {
  viewer {
    nodes(ids: $ids) {
      ...bookFragment
    }
  }
}


fragment bookFragment on Book {
  id,
  name
}
