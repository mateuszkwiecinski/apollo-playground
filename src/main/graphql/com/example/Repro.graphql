query GetLibraries {
  viewer {
    libraries(limit: 1) {
      book {
        ...book
      }
    }
  }
}


query GetBookDetails(
  $id: String!
) {
  viewer {
    book(id: $id) {
      ...book
    }
  }
}

fragment book on Book {
  id,
  name,
  year,
  author
}
