type Query {
  viewer: Viewer!
}

type Viewer {
  shelf(id: ID!): Shelf!
  node(ids: ID!): Node
  nodes(ids: [ID!]!): [Node]!
}

type Shelf {
  id: ID!
  books: [Book!]!
}

type Book implements Node {
  id: ID!
  name: String!
}

interface Node {
  id: ID!
}
