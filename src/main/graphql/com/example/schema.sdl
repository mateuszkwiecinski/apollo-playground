type Query {
  viewer: Viewer!
}

type Viewer {
  libraries(limit: Int!): [Library!]!
  book(id: String!): Book!
}

type Library {
  name: String!
  book: Book!
}

type Book {
  id: String!
  name: String!
  year: Int!
  author: String!
}
