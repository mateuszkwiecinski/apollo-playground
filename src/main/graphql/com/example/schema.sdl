type Query {
  viewer: Viewer!
}

type Viewer {
  author(id: ID!): Author!
  book(id: ID!): Book!
}

type Book {
  id: ID!
  name: String!
  author: Author!
}

type Author {
  id: ID!
  name: String!
  topBook: Book!
}
